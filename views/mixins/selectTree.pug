mixin selectTree(tree, level, parentId)
    each item in tree
        option(value=item.id selected=item.id === parentId) #{ '-- '.repeat(level) } #{ item.title }
        if item.children && item.children.length > 0
            +selectTree(item.children, level + 1, parentId)